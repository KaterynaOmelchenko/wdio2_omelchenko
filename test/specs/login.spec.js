const { expect } = require('chai');

const Page = require('./Page');

describe('LOGIN', () => {
    it('should ', () => {
        Page.open('https://webdriver.io/docs/pageobjects.html');
    }) ;
});

describe('LOGIN', () => {
    it('should ', () => {
        Page.open('https://webdriver.io/docs/pageobjects.html');
    }) ;
});